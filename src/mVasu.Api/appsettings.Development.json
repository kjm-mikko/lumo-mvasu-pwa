{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.AspNetCore": "Information",
        "Microsoft.Hosting.Lifetime": "Information",
        "System": "Warning"
      }
    }
  },
  "Cors": {
    "AllowedOrigins": [
      "https://localhost:4200",
      "http://localhost:4200"
    ]
  },
  "ConnectionStrings": {
    "_VasuDb_comment": "Set via: dotnet user-secrets set ConnectionStrings:VasuDb \"<your dev connection string>\". Do NOT commit a real value here."
  },
  "Development": {
    "_ImpersonateEmail_comment": [
      "Optional dev-only override: when set, EmailResolver returns this email",
      "for every authenticated request, letting you test as a different mVasu",
      "user without re-logging into Azure AD. Honored only in Development env.",
      "Set via user-secrets, env var, or CLI:",
      "  dotnet user-secrets set Development:ImpersonateEmail test.user@kojamo.fi",
      "  Development__ImpersonateEmail=test.user@kojamo.fi",
      "  dotnet run -- --Development:ImpersonateEmail=test.user@kojamo.fi",
      "Clear by removing the value. Active impersonation logs a WARNING at startup."
    ],
    "DevHeaderAuth": {
      "_comment": [
        "Dev-only authentication bypass. When Enabled=true, requests with",
        "an X-Dev-User: <email> header authenticate as that email WITHOUT an",
        "Azure AD JWT — see DevHeaderAuthenticationHandler. The PWA's dev",
        "user picker (environment.devAuth.enabled) writes this header.",
        "Honored only in Development env. Active scheme logs a WARNING at",
        "startup. Set via:",
        "  dotnet user-secrets set Development:DevHeaderAuth:Enabled true",
        "  Development__DevHeaderAuth__Enabled=true"
      ],
      "Enabled": false
    }
  }
}
